# Derived outputs

This directory receives reproducible text summaries generated by the simulation
scripts. The committed manuscript figures remain in `figures/`; large or
environment-specific data products should not be committed unless required for
the archival release.
